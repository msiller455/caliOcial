import React from 'react';
import { HomeContainer } from './style'

const Home = (props) => {
    return (
        <HomeContainer>
            <h1>This is the home component</h1>
        </HomeContainer>
    );
};

export default Home;